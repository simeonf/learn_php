<?php
/* Read
   - https://www.php.net/manual/en/language.types.string.php
   - https://www.php.net/manual/en/language.operators.string.php
   - https://www.php.net/manual/en/ref.strings.php
   - https://flatcoding.com/tutorials/php-programming/php-string/
*/

// Add two strings that are passed in
function add2strings($a, $b){
    return $a + $b;
}

// Put a space between first name and last name
// Make sure first and last are both capitalized
function name($first, $last){
    return "";
}

// make an rgb string out of variables "rgb(200, 100, 5)"
// don't allow an illegal value rgb(256, 300, 400)
function rgb($r, $g, $b){
    return "rgb(,,)";
}

// If long text is longer than length, insert line breaks "\n"
function wrap($long_text, $length){
    return $long_text;
}

//Only print the string if there are more "e"s than "a"s.
function more_e($text){
    return text;
}
?>
